import { ScrubDirective } from './scrub.directive';

describe('ScrubDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrubDirective();
    expect(directive).toBeTruthy();
  });
});
