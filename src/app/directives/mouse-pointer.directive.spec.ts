import { MousePointerDirective } from './mouse-pointer.directive';

describe('MousePointerDirective', () => {
  it('should create an instance', () => {
    const directive = new MousePointerDirective();
    expect(directive).toBeTruthy();
  });
});
