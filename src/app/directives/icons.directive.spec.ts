import { IconsDirective } from './icons.directive';

describe('IconsDirective', () => {
  it('should create an instance', () => {
    const directive = new IconsDirective();
    expect(directive).toBeTruthy();
  });
});
