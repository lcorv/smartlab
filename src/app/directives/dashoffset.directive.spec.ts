import { DashoffsetDirective } from './dashoffset.directive';

describe('DashoffsetDirective', () => {
  it('should create an instance', () => {
    const directive = new DashoffsetDirective();
    expect(directive).toBeTruthy();
  });
});
