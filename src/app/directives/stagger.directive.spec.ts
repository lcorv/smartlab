import { StaggerDirective } from './stagger.directive';

describe('StaggerDirective', () => {
  it('should create an instance', () => {
    const directive = new StaggerDirective();
    expect(directive).toBeTruthy();
  });
});
