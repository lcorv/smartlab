import { TextAnimationDirective } from './text-animation.directive';

describe('TextAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
